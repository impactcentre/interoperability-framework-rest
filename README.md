# interoperability-framework-rest
This is an interface to use either the soap services generated with toolwrapper tool as the taverna workflows. REST interface allows to do easier the web integration 
